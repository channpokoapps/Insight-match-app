# 未決事項リスト（Open Issues / TBD）

**最終更新**：2026-08-04

【事実】本ファイルは要件定義書全章の未決事項を集約する唯一の場所である。各章には ID とタイトルのみを記載し、内容の本体はここに書く。

---

## 凡例

| 項目 | 説明 |
|---|---|
| **影響度** | 高＝決まらないと設計・実装が進まない／中＝実装は進むが手戻りリスクあり／低＝運用開始までに決まればよい |
| **期限目安** | `設計前` ＝ 8章（データモデル）着手前／`実装前` ＝ 該当機能の実装着手前／`リリース前` ＝ 本番公開前 |
| **状態** | 未着手 / 確認中 / 決定済 |

---

## 一覧（影響度 高）

| ID | 分類 | 内容 | 推奨案 | 影響度 | 期限目安 | 状態 |
|---|---|---|---|---|---|---|
| `OI-01` | 外部連携 | Meta アプリ審査の申請主体（法人／個人事業主）と、事業者確認に必要な書類の準備状況が不明。審査に 2〜4 週間かかるため、これが決まらないと本番公開日が確定できない。 | 開発着手と同時に Meta 開発者アカウントを作成し、必要書類を洗い出す。法人での申請が難しい場合の代替案（個人開発者アカウント＋事業内容の説明）も並行検討。 | 高 | 設計前 | 未着手 |
| `OI-03` | 法務 | 利用規約・プライバシーポリシーの作成主体とレビュー体制が未定。Meta のアプリ審査ではプライバシーポリシー URL の提出が必須のため、審査申請までに必要。 | 専門家（弁護士）レビューを前提に、ドラフトは開発側で用意。特に「インサイトは PR依頼者に開示されない」旨を明記。 | 高 | 実装前 | 未着手 |
| `OI-04` | 法務／機能 | 景表法ステマ規制への対応方法。案件の指定ハッシュタグに広告表記（`#PR` 等）を必ず含めることを、システムでどこまで強制するか。 | 案件作成時に `#PR` を削除不可のタグとして自動付与し、投稿確認時にもタグの有無をチェックする。 | 高 | 実装前 | 一部対応（`0015` で作成時の自動付与・削除不可を実装。投稿確認時のチェックは未対応） |
| `OI-06` | 非機能 | 対象 OS 下限バージョンの確定。指定なしだが、下げるほど検証工数が増える。 | Flutter / 主要ライブラリの標準サポート範囲に合わせる（Android は minSdk を Flutter 既定値のまま採用）。実装着手時点の一次情報で確定する。 | 高 | 実装前 | 未着手 |
| `OI-08` | 法務／機能 | 投稿者の年齢制限。未成年（特に 18 歳未満）の登録を認めるか、認める場合の保護者同意の取得方法。 | 初期は **18 歳以上のみ**とし、登録時に生年月日入力と年齢確認チェックを行う。未成年対応は将来検討。 | 高 | 設計前 | 未着手 |
| `OI-12` | 機能 | 投稿が実際に行われたことをどう確認するか。ハッシュタグでの自動検出は API 制約上難しい可能性がある。 | 投稿者が投稿 URL（またはメディアID）をアプリで提出 → サーバーが本人の連携アカウントのメディア一覧と突合して検証する方式を推奨。 | 高 | 設計前 | 未着手 |
| `OI-17` | セキュリティ | SNS アクセストークンの暗号化方式と鍵管理。 | **Edge Function 内 AES-256-GCM + 鍵は Supabase Secrets（`TOKEN_ENCRYPTION_KEY`）で決定・実装済み**（2026-08、[ADR-0006](adr/0006-token-encryption.md)。`_shared/crypto.ts` に集約）。 | 高 | 設計前 | **決定済み** |
| `OI-22` | 機能 | 未ログイン状態で案件一覧を閲覧できるようにするか。集客上は有利だが、条件合致判定ができないため全件モザイクになる。 | 未ログインでは「案件が◯件あります」という件数とジャンル分布のみ見せ、一覧は見せない。 | 高 | 設計前 | 未着手 |
| `OI-36` | 法務／技術 | 退会・SNS連携解除時にインサイトを削除すると、過去案件の成果レポートが再生成できなくなる。削除義務とレポートの整合性をどう両立するか。 | 案件完了時点で **k-匿名性を満たした集計スナップショット**を保存し、個人の生データを削除してもレポートが壊れない設計にする。スナップショットは個人情報を含まないため保持できる。 | 高 | 設計前 | 未着手 |
| `OI-37` | 外部連携 | Instagram Graph API で実際に取得できるインサイト指標の最終確認。9-2 の表は変動しうる仕様に基づく。 | 実装着手時に開発モードで実データを取得し、9-2 の表を確定させる。取得できない指標は条件式の選択肢から外す。 | 高 | 実装前 | 未着手 |
| `OI-43` | 技術／機能 | チャットのリアルタイム配信方式。Supabase の `postgres_changes` は行全体を配信するため、`public.messages` を Realtime に公開すると PR依頼者に投稿者の `sender_id` が届いてしまう（R-5 違反）。 | 当面は `messages` を Realtime に公開せず、`v_chat_messages` の再取得（ポーリング／画面復帰時リフレッシュ）で代替する。将来は Realtime Broadcast で「新着あり」の通知のみを送り、本文はビューから取得する方式を推奨。 | 高 | 実装前 | 未着手 |

---

## 一覧（影響度 中）

| ID | 分類 | 内容 | 推奨案 | 影響度 | 期限目安 | 状態 |
|---|---|---|---|---|---|---|
| `OI-02` | スコープ | TikTok / YouTube 連携の対応時期。初期リリースに含めるか、Instagram の価値検証後にするか。 | 初期は Instagram のみ。ただし DB は複数 SNS 対応（`social_accounts` テーブルをプラットフォーム識別子で汎化）で設計する。 | 中 | 設計前 | 未着手 |
| `OI-05` | 事業 | サービス運営主体（法人格・所在地）。利用規約・ストア申請・Meta 審査すべてで必要。 | — | 中 | リリース前 | 未着手 |
| `OI-07` | 事業／機能 | 広告収益の実装方式（AdMob 等）と表示位置。投稿者側のみに出すか、PR依頼者側にも出すか。 | 投稿者側の案件一覧にネイティブ広告を挿入。PR依頼者側には出さない（有料機能の顧客候補のため）。 | 中 | 実装前 | 未着手 |
| `OI-09` | 技術 | 案件の地理検索に必要な、住所 → 緯度経度変換（ジオコーディング）サービスの選定。 | PostgreSQL の `earthdistance` / PostGIS で距離計算し、ジオコーディングは案件登録時に 1 回だけ外部 API を呼ぶ（呼び出し回数を抑えコストを最小化）。 | 中 | 設計前 | 未着手 |
| `OI-10` | 技術／法務 | 路線・駅データの入手元とライセンス（商用利用可否）。 | **決定・実装済み**（2026-08）。市区町村は [code4fukui/address-japan](https://github.com/code4fukui/address-japan)（MIT、デジタル庁アドレス・ベース・レジストリ由来）、路線・駅は [Seo-4d696b75/station_database](https://github.com/Seo-4d696b75/station_database)（**CC BY 4.0**）。`scripts/build_master_data.mjs` が `0012` / `0013` を生成する。CC BY の表示義務は設定の「データ出典」画面で果たす。 | 中 | 設計前 | **決定済み** |
| `OI-11` | 技術 | プッシュ通知基盤の選定。 | FCM（無料枠が大きく Flutter との親和性が高い）。Supabase 側から Edge Function 経由で送信。 | 中 | 実装前 | 未着手 |
| `OI-13` | 運用 | キャンセル（特に直前キャンセル）時のペナルティ運用ルール。 | 初期はペナルティを課さず、キャンセル率を記録するのみ。運営が悪質な繰り返しを検知して手動で対応。 | 中 | 実装前 | 未着手 |
| `OI-14` | 機能 | 募集人数が k（=5）未満の案件で、成果レポートをどう扱うか。数値を一切出さないと PR依頼者の体験が損なわれる。 | 個別数値は出さず、「合計値のみ（合計リーチ等）」を表示する案と、「非表示」案を比較。合計値でも人数が 1 人だと個人特定になるため、**n=1 では完全非表示**を最低ラインとする。 | 中 | 設計前 | 未着手 |
| `OI-15` | 技術 | 運営管理画面の実装形態。Flutter Web で作るか、Supabase Studio や外部の管理画面ツールで代替するか。 | 初期は Supabase Studio ＋ 最小限の SQL 運用で代替し、通報対応の件数が増えた段階で Flutter Web の管理画面を作る（開発コスト最小化）。 | 中 | 実装前 | 未着手 |
| `OI-18` | スコープ | 対応 SNS の拡張順序（TikTok 先か YouTube 先か）。 | 投稿者の登録時アンケートで需要を測ってから決定。 | 中 | リリース前 | 未着手 |
| `OI-20` | 機能 | 応募をキャンセルした投稿者が、同じ案件に再応募できるか。 | 不可とする（募集人数の計算とマッチング状態が複雑になるため）。 | 中 | 実装前 | 未着手 |
| `OI-23` | 運用／セキュリティ | 障害調査等でインサイト実数値へ運営がアクセスする必要が生じた場合の例外運用ルール。 | アプリ・管理画面には一切表示経路を作らない。DB 直接アクセスは 2 名承認制とし、アクセスログを残す。 | 中 | リリース前 | 未着手 |
| `OI-24` | 機能 | SNS 連携直後、翌日のバッチを待たずに初回インサイトを即時取得するか。 | **即時取得する**ことを推奨。連携直後に案件が全部モザイクのままだと離脱率が高くなるため。ただしレート制限に配慮しキューイングする。 | 中 | 設計前 | 未着手 |
| `OI-25` | 運用 | 新規 PR依頼者の初回案件に事前確認を入れるか。 | 事前確認は入れず（承認制ではないという要件を尊重）、運営ダッシュボードで新規依頼者の案件を強調表示する事後確認方式。 | 中 | 実装前 | 未着手 |
| `OI-26` | 技術／セキュリティ | 認証方式の確定（パスワード要件、MFA、ソーシャルログインの可否）。 | creator/client は**メール＋パスワードに加えて Google サインインを提供**（2026-08 決定・実装済み。パスワードは 8 文字以上・英数字混在）。admin は MFA 必須。Instagram はログイン手段にしない。 | 中 | 設計前 | **決定済み** |
| `OI-29` | 機能／セキュリティ | モザイク案件で「どの条件をどれだけ下回っているか」を投稿者に開示するか。開示すると改善の動機になるが、複数案件の条件と自分の合否から分布を推測する余地が生まれる。 | **開示しない。** 代わりに「マイインサイト」画面で自分の数値と一般的な目安を示し、改善を促す。 | 中 | 設計前 | 未着手 |
| `OI-30` | 機能／運用 | 選考モードで PR依頼者がいつまでも確定しない場合の期限と自動処理。 | 選考開始から 7 日で自動的に「応募順の上位から募集人数分をマッチング成立」とする案と、「案件を中止」とする案を比較。投稿者を待たせ続けない仕組みは必須。 | 中 | 設計前 | 未着手 |
| `OI-31` | 機能／運用 | キャンセル申請が却下された後、投稿者が実際に履行できない場合の扱い。 | 却下後も投稿がないまま投稿期間が終了したら自動的に「未投稿終了」とし、記録を残す。ペナルティは課さない（`OI-13` と連動）。 | 中 | 実装前 | 未着手 |
| `OI-33` | UI | デザインシステム／UI キットの選定（Material 3 準拠か独自か）。 | Material 3 をベースに最小限のカスタムテーマ。独自デザインシステムの構築は初期スコープ外。 | 中 | 実装前 | 未着手 |
| `OI-34` | 機能／セキュリティ | 条件緩和の提案で「増加見込み人数」を提示する際、実数を出すと二分探索攻撃の材料になる。 | 増加人数も 5 未満は「5人未満」に丸める。緩和幅の刻みも固定値（例：10%・20%・30%）に限定し、任意の閾値を試せないようにする。 | 中 | 設計前 | 未着手 |
| `OI-35` | 機能 | オーディエンス属性（性別・年齢・地域）を案件の条件に使えるようにするか。 | 初期スコープでは条件に含めない。取得・保存は行い、将来の条件追加に備える。属性を条件にすると差別的な募集になりうるため、ガイドラインの整備が先。 | 中 | 設計前 | 未着手 |
| `OI-38` | 技術／法務 | インサイト生データの保存期間。90 日案の妥当性。 | 90 日平均の算出に必要な範囲＋余裕を見て 120 日とし、超過分は日次で削除。プラットフォームのデータ保持ポリシーも確認する。 | 中 | 設計前 | 未着手 |
| `OI-39` | コスト | 11章のコスト試算は概算。各サービスの最新価格の確認。 | 採用決定前に Supabase / FCM / ジオコーディング / 広告 SDK の最新価格を確認し、試算を確定する。 | 中 | 実装前 | 未着手 |
| `OI-45` | 技術／運用 | 本番 Supabase（マイグレーション・Edge Functions）を main マージで自動デプロイするか。現状は Hosting のみ自動で、`supabase/` の変更は手動反映（`deploy_production.yml` が PR に警告コメントを出す）。 | PoC 期は手動を維持（R-1〜R-5 の防衛線を人間の操作に残す）。CI の `supabase test db` が同一 SHA で通っていることを前提に、利用者が増える前に自動化を再検討する。自動化する場合は `SUPABASE_ACCESS_TOKEN` / DB パスワードを Secrets に追加し `supabase db push` を CI から実行する。 | 中 | リリース前 | 未着手 |
| `OI-44` | 事業／技術 | サービス名・パッケージ名の確定。ストア申請・Meta 審査・GA4 設定の前提。 | サービス名 **SNS Insight Matcher** / Dart パッケージ `insight_match` / applicationId `app.insightmatch.android` で決定（詳細は [manual_setup/naming.md](manual_setup/naming.md)）。 | 中 | リリース前 | **決定済み** |
| `OI-47` | デザイン | アプリアイコンが Flutter 既定のまま（`app/android/.../ic_launcher.png`、`app/web/icons/Icon-*.png`、`app/assets/images/` は空）。認証メールのヘッダは `{{ .SiteURL }}/icons/Icon-192.png` を参照しているため、差し替えるとメールの見た目にも反映される。 | ブランドカラー（`AppTheme.brandGradient`）を用いた独自アイコンを作成し、Android / Web の全サイズを差し替える。ストア申請前に必要。 | 中 | リリース前 | 未着手 |
| `OI-46` | 技術 | Flutter の `AppFailure.from` が、SQL 側 `raise exception` の**日本語文言の文字列一致**で例外種別を判定している（`app_failure.dart` / `0005_functions.sql`）。SQL のメッセージを変えると Flutter 側が黙って `unknown` に落ち、利用者向けの案内が失われる。 | `raise exception ... using errcode = '...'` でカスタム errcode を割り当て、Flutter 側は PostgrestException の `code` で分岐する方式へ移行する。 | 中 | 実装前 | 未着手 |

---

## 一覧（影響度 低）

| ID | 分類 | 内容 | 推奨案 | 影響度 | 期限目安 | 状態 |
|---|---|---|---|---|---|---|
| `OI-16` | 技術／コスト | ステージング環境を持つか。 | 初期は持たない（ローカルスタックで代替）。ユーザー数が増えた段階で追加。 | 低 | リリース前 | 未着手 |
| `OI-19` | 運用 | 投稿者・PR依頼者の本人確認（KYC）の要否。 | 初期は不要。将来の有償案件・決済導入時に必須化を検討。 | 低 | リリース前 | 未着手 |
| `OI-21` | 事業 | KPI 目標値の確定（[01_introduction.md](requirements/01_introduction.md) 1-5 の数値はすべて仮）。 | リリース後 1 か月の実績をベースラインとして再設定。 | 低 | リリース前 | 未着手 |
| `OI-27` | 体制 | 開発体制の人数・期間。 | — | 低 | 設計前 | 未着手 |
| `OI-28` | 体制 | カスタマーサポート体制と通報対応の SLA。 | 初期は運営 1 名が営業日 2 日以内に対応する想定でドキュメント化。 | 低 | リリース前 | 未着手 |
| `OI-32` | 機能 | 案件完了後にチャットをどれだけ保持するか。 | 完了後 30 日で読み取り専用、90 日で非表示。メッセージ本体はトラブル対応のため 1 年保持。 | 低 | 実装前 | 未着手 |
| `OI-40` | 法務／事業 | 有償案件を導入する際のストア規約（外部決済の可否）と決済方式の確認。 | 報酬は「役務提供の対価」であり一般にストア内課金の対象外と考えられるが、実装前にストア規約と法務の確認が必須。決済は自前実装せず決済代行に乗る。 | 低 | 将来 | 未着手 |
| `OI-41` | 機能 | 条件式に、そのプラットフォームで取得できない指標が含まれる場合の扱い。 | 条件作成 UI の選択肢から取得不可の指標を除外する。既存条件に含まれる場合は「不合致」として扱う。 | 低 | 実装前 | 未着手 |
| `OI-42` | 機能 | 1 アカウントで複数店舗を管理する要件（フランチャイズ・多店舗展開）の有無。 | 初期は 1 ユーザー 1 店舗。将来は `client_profiles` を `organizations` と `stores` に分割する拡張余地を残す。 | 低 | 将来 | 未着手 |

---

## 決定済み事項（記録）

| ID | 内容 | 決定 | 決定日 |
|---|---|---|---|
| — | モバイル技術スタック | **Flutter** | 2026-07-31 |
| — | バックエンド／DB | **Supabase**（PostgreSQL + RLS + Edge Functions） | 2026-07-31 |
| — | 日次バッチ実行基盤 | **Supabase pg_cron ＋ Edge Function** | 2026-07-31 |
| — | PR依頼者に見せる投稿者の匿名表示 | **案件内で閉じた連番**（投稿者A / B / C…）。案件をまたいだ名寄せは不可 | 2026-07-31 |
| — | k-匿名性の閾値 | **k = 5** | 2026-07-31 |
